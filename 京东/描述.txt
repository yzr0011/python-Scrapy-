这个爬虫是一个未完成的爬虫，只是把第一个页面的商品名称与价格爬出来，并未写爬下一页的代码，京东刚开始我是准备通过列表页爬取详情页url，再通过scrapy的get得到详情页数据用xpath解析，后来发现京东价格数据是用ajax动态渲染的（原谅我只是小白），然后急匆匆去写了个中间件，selenium中间件，但是发现只能执行到第一个函数，后面的回调函数就不能获取数据了。。后来我设置了if ！= 起始url：这个条件，执行是执行了回调函数，页面也出来了，但返回数据一直报错。。。。于是乎我去b站看了下大佬们怎么爬京东的，发现还可以通过分析网页的动态json数据的方式获取到想要的数据


（具体怎么分析的，我也写不清楚，大致就是找到含价格链接（用谷歌浏览器f12），然后在链接上修改那些数字，如果改了以后数据没有变化，就把那段链接删掉，这样链接就越改越短，最后改成了“一段链接”+（列表页面可获得的商品id）+“一段链接”的模式）

。。。。。然后就是正常的转str json.loads  等

然后最终通过列表方式取值
